# SAUNA BASE — CLAUDE.md

グローバルルール（`/Users/yukiota/Desktop/Clans Quest Project/CLAUDE.md`）を前提とし、
以下をこのプロジェクト固有のルールとして補足する。

---

## 1. このプロジェクトの役割

「あなたのととのいを点数化するアプリ」。サウナ体験を記録・可視化・最適化するライフログWebアプリ。
個人利用・プロトタイプフェーズ。現在はモックデータで動作中。

---

## 2. 基本情報

| 項目 | 値 |
|------|----|
| 本番URL | 未デプロイ（Vercel連携予定） |
| GitHub | 未設定 |
| Supabase | 将来連携予定（現在はモックデータ） |
| ローカルパス | `/Users/yukiota/Desktop/Clans Quest Project/sauna-base` |
| 担当AI | Webアプリ開発AI（webapp-dev-ai） |

---

## 3. 技術スタック

- Next.js 16（App Router + TypeScript）
- Tailwind CSS v4（v3とは設定方法が異なる点に注意）
- recharts（グラフ・可視化）
- lucide-react（アイコン）
- Supabase（将来連携予定 — 現在は未使用）

---

## 4. 現在のフェーズ（重要）

**現在: プロトタイプフェーズ / モックデータ動作中**

- Supabase 未連携（データは全てモック）
- 認証なし（将来追加予定）
- PWA 未対応（将来追加予定）

**次フェーズで追加予定:**
- Supabase 連携（データ永続化）
- ユーザー認証
- PWA対応

---

## 5. ページ構成

| パス | 概要 |
|------|------|
| `/` | ダッシュボード（スコア・統計・カレンダー・AIレコメンド） |
| `/record` | サウナ記録フォーム |
| `/calendar` | カレンダー |
| `/stats` | 統計・記録一覧 |
| `/ai` | AIレコメンド（ルールベース） |
| `/profile` | サウナプロファイル |

---

## 6. ディレクトリ責務

```
sauna-base/
├── app/                 # Next.js App Router
│   ├── page.tsx         # ダッシュボード
│   ├── record/          # 記録フォーム
│   ├── calendar/        # カレンダー
│   ├── stats/           # 統計
│   ├── ai/              # AIレコメンド
│   └── profile/         # プロファイル
├── components/          # UIコンポーネント
│   ├── Sidebar.tsx      # ナビゲーション
│   └── [機能別コンポーネント]
└── lib/                 # ユーティリティ（将来Supabaseクライアントもここ）
```

---

## 7. AI行動ルール

1. **モックデータを本番DBと混同しない** — 現在のデータは全てハードコードされたモック
2. **Tailwind v4 の書き方に従う** — `@import "tailwindcss"` 形式。v3との差異に注意
3. **Supabase連携は次フェーズ** — 現フェーズでは Supabase クライアントを追加しない
4. **既存のスコア計算ロジックを確認してから変更する** — ととのいスコアの計算式は仕様の核心
5. **recharts の既存グラフパターンを流用する** — 新しいグラフはまず既存コンポーネントを参照

---

## 8. 実装禁止事項

- 現フェーズで Supabase クライアントを追加すること（フェーズ管理を守る）
- モックデータのハードコードを散在させること（lib/ または定数ファイルにまとめる）
- Tailwind v3 の設定ファイル書式（`tailwind.config.js`）をこのプロジェクトに追加すること

---

## 9. ととのいスコア仕様（重要）

ととのいスコアは以下の要素から算出する。スコア計算ロジックを変更する際は必ずユーザーに確認する。

- サウナ時間・水風呂時間・外気浴時間のバランス
- セット数
- 体感の自己評価

---

## 10. 将来フェーズで追加する際の注意

Supabase連携を追加する際は:
1. `lib/supabase/client.ts` + `lib/supabase/server.ts` の**ディレクトリ型パターン**を使う
2. `operations/supabase-standard.md` の標準実装を参照する
3. middleware.ts を追加して認証ガードを設ける
